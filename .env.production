# Production builds never ship the [DAWN][DAY][DUSK][NIGHT] preview buttons,
# whatever a local .env says — Vite gives .env.production precedence over
# .env for `vite build`, while `vite dev` (mode=development) still honours it.
VITE_SHOW_ENV_DEBUG=false
